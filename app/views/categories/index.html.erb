<h1>Pick from a Category to see a Recipe:</h1>
<table>
  <% @categories.each do |category| %>
  <tr>
    <td><%= link_to "#{category.name}", category_path(category) %></td>
  </tr>
  <% end %>
</table>